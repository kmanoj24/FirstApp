
<% if @user.errors.any? %>
	<div class="alert alert-danger">
		<% @user.errors.full_messages.each do |message| %>
			<div><%= message %></div>
		<% end %>			
	</div>
<% end %>
<div class="col-lg-4 mx-lg-auto">
  	<div class="card shadow">
	    <div class="card-header">
	      <h4 class="card-title">Sign Up</h4>
	    </div>
	    <div class="card-body">
	    	<!-- <%#= form_for @user, url: users_path, method: :post, data: { turbo: false } do |f| %>				
				<div class="form-inputs">
					Name: <%#= f.text_field :name, class: "form-control form-control-sm" %>
					<br>
					Email: <%#= f.email_field :email, class: "form-control form-control-sm" %>
					<br>

					Password: <%#= f.password_field :password, class: "form-control form-control-sm" %><br>
				</div>
				<div class="actions">
					<%#= f.submit 'Register', class: "btn btn-primary btn-sm"  %>
				</div> -->
			<%# end %>
			
		</div>
	</div>
</div>
